# Go test/coverage artifacts
*.out
coverage*.out
*_cov.out

# OS
.DS_Store

# Vendoring (not used)
vendor/

# IDE
.idea/
.vscode/

# =============================================================================
# SDK Examples (build artifacts)
# =============================================================================

# Compiled example binaries — named after their containing directory.
# These are `go build` artifacts, never source. Ignore explicitly so they
# are not accidentally committed again (they were removed from the index once).
examples/01-minimal-agent/01-minimal-agent
examples/02-custom-tools/02-custom-tools
examples/03-event-streaming/03-event-streaming
examples/04-human-in-the-loop/04-human-in-the-loop
examples/05-mcp-integration/05-mcp-integration
examples/06-plan-and-reflect/06-plan-and-reflect
examples/07-full-power/07-full-power

# =============================================================================
# Cross-repo development workspace (never committed)
# =============================================================================
# go.work wires this module to a local checkout of github.com/v0lka/flowsh
# during a cross-repo development cycle; the module still pins the published
# flowsh commit in go.mod. Keep it untracked (see c0wrk ADR-031).
go.work
go.work.sum
